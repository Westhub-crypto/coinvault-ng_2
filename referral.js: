const express = require('express');
const router = express.Router();

router.post('/', (req, res) => {
    const { referrerId, refereeId } = req.body;
    res.json({ success: true, commission: 5 });
});

module.exports = router;